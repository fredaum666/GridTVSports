# VowsSite Database Configuration
# This is a SEPARATE database from the GridTVSports database

# PostgreSQL connection URL for VowsSite (separate from GridTVSports)
# Format: postgresql://username:password@host:5432/database_name?sslmode=require
VOWS_DATABASE_URL=postgresql://username:password@localhost:5432/vows_wedding?sslmode=require

# API Port (default: 3001, different from GridTVSports main API)
VOWS_API_PORT=3001

# Notes:
# - This database should be separate from the GridTVSports DATABASE_URL
# - You can use the same PostgreSQL server but a different database name
# - Example: If GridTVSports uses "gridtv_sports" database, use "vows_wedding" for VowsSite
# - This separation allows easy table exclusion later
