# Gradle build files
.gradle/
build/

# Local properties (contains SDK path)
local.properties

# Keystore credentials (NEVER commit this!)
keystore.properties

# OS files
.DS_Store
Thumbs.db

# APK files (build outputs)
*.apk
*.aab
